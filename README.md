# Compass Consult Website

[![License: MIT](https://img.shields.io/badge/License-MIT-purple.svg)](LICENSE)
[![Live Site](https://img.shields.io/badge/Live%20Site-compassconsultes.co.uk-teal)](https://compassconsultes.co.uk)
[![Netlify Status](https://api.netlify.com/api/v1/badges/compass-consult/deploy-status)](https://app.netlify.com/sites/compass-consult/deploys)

Official website for **Compass Consult (Employability & Skills) Ltd** — a
professional consultancy specialising in skills training, employability
programmes, and career development services.

Live site: **[compassconsultes.co.uk](https://compassconsultes.co.uk)**

---

## 🌍 Overview

Compass Consult (Employability & Skills) Ltd works with individuals,
employers, and public sector authorities to deliver tailored employability
and skills programmes across the UK. This repository contains the full
source code for the company's public-facing website.

The site is a **fully responsive static website** built without any
front-end framework. It is structured for clarity, performance, and
scalability, and is deployed on Netlify with serverless functions for
form handling and subscriber management.

### ✨ Key Features

- **Fully responsive** — mobile-first layout with a dedicated sidebar
  navigation on desktop and a slide-in hamburger menu on mobile
- **Progressive Web App (PWA)** — installable on mobile and desktop via
  a Service Worker (`sw.js`) and web app manifest
- **Accessibility-first** — skip navigation links, semantic HTML,
  ARIA attributes, keyboard-navigable menus, and `prefers-reduced-motion`
  support throughout
- **Performance optimised** — locally built and versioned CSS/JS assets
  with long-lived cache headers; WebP images with lazy loading
- **SEO ready** — XML sitemap (auto-generated by CI), `robots.txt`,
  Open Graph meta tags, and structured Schema.org markup
- **Security hardened** — CSP, `X-Frame-Options`, `X-Content-Type-Options`,
  and other security headers enforced via `netlify.toml`
- **Netlify Forms** — contact and newsletter forms with zero-config
  spam filtering
- **Netlify Functions** — serverless TypeScript function for subscriber
  management backed by Netlify Blobs storage
- **Automated CI/CD** — GitHub Actions for linting, link checking, and
  sitemap/robots.txt regeneration on every push to `main`

---

## 🛠 Tech Stack

| Technology | Version | Purpose |
|---|---|---|
| HTML5 | — | Semantic page markup |
| Tailwind CSS | 3.x | Utility-first styling — built locally via CLI |
| @tailwindcss/typography | 0.5.x | Prose styles for rich-text content blocks |
| Vanilla JavaScript | ES2020+ | Interactivity, animations, and accessibility |
| Lucide Icons | 1.x | SVG icon library — bundled locally via esbuild |
| AOS (Animate On Scroll) | 2.x | Scroll-triggered entrance animations |
| Netlify Forms | — | Contact and newsletter form handling |
| Netlify Functions | — | Serverless TypeScript (subscriber management) |
| Netlify Blobs | — | Edge-native key-value storage for subscribers |
| Service Worker | — | PWA offline support and asset caching |
| Node.js / npm | ≥ 16 | Build tooling and dev server |
| ESLint | 9.x | JavaScript linting |
| sharp | 0.34.x | Server-side image optimisation script |

**Brand colours:**

| Token | Hex | Use |
|---|---|---|
| `compass-purple` | `#483086` | Primary — headings, buttons, sidebar |
| `compass-lightPurple` | `#6a4eb8` | Hover states |
| `compass-teal` | `#2da8b4` | Accent — highlights, links |
| `compass-lightTeal` | `#4fd3e0` | Hover states |
| `compass-dark` | `#1e1b2e` | Dark backgrounds |
| `compass-light` | `#f8f9fc` | Light backgrounds |

---

## 📂 Project Structure

```
Compass-Consult-Website/
├── index.html                        # Main homepage (entry point)
│
├── pages/                            # Secondary HTML pages
│   ├── about.html                    # About the company
│   ├── company-information.html      # Company registration & legal details
│   ├── contact.html                  # Contact form and office details
│   ├── cookies.html                  # Cookie policy
│   ├── events.html                   # News & events
│   ├── partnerships.html             # Partnerships & accreditations
│   ├── privacy.html                  # Privacy policy (GDPR)
│   ├── resources.html                # Downloadable resource library
│   ├── services.html                 # Full services listing
│   ├── sitemap.html                  # HTML sitemap for visitors
│   ├── team.html                     # Team member profiles
│   ├── terms.html                    # Terms of service
│   └── work-for-us.html              # Careers / vacancies
│
├── css/
│   ├── input.css                     # Tailwind CSS entry file
│   ├── main.css                      # Custom styles, animations, layout fixes
│   └── tailwind.min.[hash].css       # Built & versioned Tailwind output
│
├── js/
│   ├── main.js                       # Shared JS behaviour (fully documented)
│   ├── aos.min.[hash].js             # Versioned AOS bundle
│   └── lucide.min.[hash].js          # Versioned Lucide Icons bundle
│
├── assets/
│   ├── images/                       # Site images (organised by category)
│   │   ├── authorities/              # Client / authority logos
│   │   ├── events/                   # Event images
│   │   ├── partners/                 # Partner logos
│   │   ├── resource-previews/        # Resource document preview images
│   │   ├── team/                     # Team member profile photos
│   │   └── work-for-us/              # Careers page imagery
│   ├── icons/favicon/                # Favicon files and PWA web manifest
│   ├── logos/                        # Brand logos (Logo.png, compass_rose)
│   └── downloads/                    # PDFs (factsheets, vacancy documents)
│       ├── docs/                     # General documentation downloads
│       └── vacancies/                # Vacancy / job description PDFs
│
├── data/                             # JSON content (single source of truth)
│   ├── README.md                     # Schema reference for JSON files
│   ├── clients.json                  # Client / authority data
│   ├── services.json                 # Service descriptions
│   └── team.json                     # Team member data
│
├── netlify/                          # Netlify platform configuration
│   ├── functions/
│   │   └── subscribers.mts           # Serverless subscriber management function
│   └── lib/
│       └── data.ts                   # Shared data utilities for functions
│
├── scripts/                          # Node.js build and utility scripts
│   ├── build-lucide.mjs              # Bundle Lucide Icons with esbuild
│   ├── generate-pwa-icons.mjs        # Generate PWA icon set from source logo
│   ├── link-check.mjs                # Check for broken internal/external links
│   ├── optimize-images.mjs           # Batch-optimise images with sharp
│   ├── sync-resources.mjs            # Sync downloadable resource files
│   ├── version-assets.mjs            # Add content hashes to asset filenames
│   └── perf-update.mjs               # Performance audit helper
│
├── docs/
│   ├── DEVELOPMENT.md                # Local dev setup and coding standards
│   └── DEPLOYMENT.md                 # Deployment procedures and CI/CD notes
│
├── .github/
│   └── workflows/
│       ├── check.yml                 # Lint & link check on push / PR
│       ├── main.yml                  # Auto-generate sitemap.xml & robots.txt
│       └── sync-resources.yml        # Sync resource files workflow
│
├── .githooks/                        # Git hooks (install with npm run hooks:install)
├── .gitignore
├── assets-manifest.json              # Maps original asset names → versioned names
├── eslint.config.mjs                 # ESLint flat config
├── netlify.toml                      # Netlify build, headers, and functions config
├── package.json                      # npm scripts and project metadata
├── robots.txt                        # Crawl directives (auto-generated by CI)
├── sitemap.xml                       # XML sitemap (auto-generated by CI)
├── sw.js                             # Service Worker for PWA support
├── tailwind.config.js                # Tailwind CSS configuration
├── CONTRIBUTING.md                   # Contribution guidelines
├── STRUCTURE.md                      # Detailed folder structure reference
└── README.md                         # This file
```

See [STRUCTURE.md](STRUCTURE.md) for a full description of every folder and file.

---

## 🚀 Quick Start

### Prerequisites

- [Node.js](https://nodejs.org/) 16 or higher
- A modern code editor ([VS Code](https://code.visualstudio.com/) recommended)
- Git

### Run locally

```bash
# 1. Clone the repository
git clone https://github.com/Jake-Gibbons/Compass-Consult-Website.git
cd Compass-Consult-Website

# 2. Install dependencies
npm install

# 3. (Optional) Install Git hooks
npm run hooks:install

# 4. Start the local dev server
npm run serve
# → http://localhost:8000

# 5. (Optional) Watch and rebuild Tailwind CSS on change
npm run watch:css
```

---

## 🧰 Available Scripts

All scripts are defined in `package.json` and run with `npm run <script>`.

| Script | Description |
|---|---|
| `npm run serve` | Start local dev server on port 8000 (no cache) |
| `npm run dev` | Start local dev server (default port, with caching) |
| `npm run build` | Full production build: CSS → Lucide → asset versioning |
| `npm run build:css` | Compile and minify Tailwind CSS |
| `npm run build:lucide` | Bundle Lucide Icons with esbuild |
| `npm run build:assets` | Version all static assets (adds content hashes) |
| `npm run watch:css` | Watch and rebuild Tailwind CSS on file changes |
| `npm run lint` | Run ESLint on all JS files |
| `npm run lint:js` | Same as `lint` |
| `npm run link-check` | Check for broken internal and external links |
| `npm run check` | Run `lint` and `link-check` together |
| `npm run optimize:images` | Batch-optimise images using sharp |
| `npm run generate:pwa-icons` | Generate the full PWA icon set from the source logo |
| `npm run sync:resources` | Sync downloadable resource files |
| `npm run hooks:install` | Register `.githooks/` as the local Git hooks directory |

---

## 🏗 Architecture Notes

### Navigation

Every page uses the same two-piece navigation pattern:

| Element | Visible at |
|---|---|
| `<aside>` — fixed left sidebar | Desktop (≥ 1024 px) |
| `<nav>` — top bar + hamburger menu | Mobile (< 1024 px) |

The hamburger menu is wired up in `js/main.js` → `initializeMobileMenu()`.

### JavaScript (`js/main.js`)

`main.js` is the single shared script loaded by every page. It runs all
initialisers on `DOMContentLoaded`. Each function is documented with JSDoc
comments. Key functions:

| Function | Purpose |
|---|---|
| `initializeMobileMenu()` | Hamburger menu toggle with ARIA support |
| `initializeStickyTalkButton()` | Floating "Talk to Us" CTA button |
| `initializeRevealAnimations()` | IntersectionObserver scroll reveals |
| `initializeBioReadMore()` | Expandable team bio sections |
| `enhanceExternalLinks()` | `noopener`/`noreferrer` + aria-label injection |
| `connectFormLabels()` | Programmatically links `<label>` to `<input>` |
| `optimizeImages()` | Adds lazy-loading and async decoding attributes |
| `initializeAOS()` | Starts the Animate On Scroll library |

A global `window.CompassConsult` object exposes `scrollToElement`,
`debounce`, `isInViewport`, and the two animation initialisers for use
by individual page scripts.

### Styling (`css/main.css`)

`main.css` supplements Tailwind with:

- CSS custom properties for the brand colour palette
- Hamburger icon animation (three bars → ×)
- Mobile menu slide-in transition
- Sticky "Talk to Us" button and tooltip styles
- Scroll-triggered reveal animation utilities
- Reduced-motion media query overrides

Tailwind CSS is **built locally** (not loaded from CDN) using the CLI:
`npm run build:css` compiles `css/input.css` → `css/tailwind.min.css`,
and `npm run build:assets` then appends a content hash for cache busting.

See the table of contents at the top of `main.css` for a full section list.

### Data Files (`data/`)

JSON files store team member details, service descriptions, and client
information as a single source of truth for the site's content. When
updating content, edit both the JSON file and the corresponding HTML page
to keep them in sync. See [`data/README.md`](data/README.md) for the
full field schema for each file.

### Netlify Functions (`netlify/functions/`)

`subscribers.mts` is a TypeScript serverless function that handles
newsletter subscription requests. It persists subscriber records to
Netlify Blobs and is bundled automatically by Netlify's esbuild bundler
on deployment.

### Asset Versioning

`scripts/version-assets.mjs` reads each asset file, computes a truncated
SHA-256 content hash, and renames the file (e.g.
`tailwind.min.css` → `tailwind.min.a1b2c3d4.css`). An `assets-manifest.json`
file maps original names to versioned names so HTML files can reference
the correct filename. This enables long-lived `Cache-Control: immutable`
headers without stale-cache issues.

### Progressive Web App

`sw.js` is a Service Worker that pre-caches the homepage shell on install
and serves cached assets on subsequent visits. The PWA manifest in
`assets/icons/favicon/` declares the app name, icons, theme colour, and
display mode so the site can be added to a device's home screen.

---

## 🚀 Deployment

The site is deployed to **Netlify** on every push to `main`.
Build settings and HTTP headers are defined in [`netlify.toml`](netlify.toml).

The Netlify build command is `npm run build:css`. Lucide Icons and asset
versioning are pre-built and committed, so the Netlify build step only
needs to recompile Tailwind CSS.

GitHub Actions run two additional automation workflows on push to `main`:

| Workflow | File | Purpose |
|---|---|---|
| Lint & Link Check | `check.yml` | ESLint + broken-link audit (Node 18 & 20) |
| Sitemap & Robots | `main.yml` | Auto-generate `sitemap.xml` and `robots.txt` |

### Other hosting options

| Platform | Notes |
|---|---|
| **Netlify** | Primary host — built-in form handling and serverless functions |
| **Vercel** | Zero-config static hosting with global CDN |
| **GitHub Pages** | Free; Settings → Pages → select `main` branch |
| **FTP / cPanel** | Upload all files to `public_html` via FileZilla or Cyberduck |

See [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md) for detailed deployment
instructions including environment variables, DNS configuration, and
rollback procedures.

---

## 🔒 Security

Security headers are enforced globally via `netlify.toml`:

- `Content-Security-Policy` — restricts resource origins
- `X-Frame-Options: DENY` — prevents clickjacking
- `X-Content-Type-Options: nosniff` — prevents MIME sniffing
- `Referrer-Policy: strict-origin-when-cross-origin`
- `Permissions-Policy` — disables camera, microphone, and geolocation

---

## ♿ Accessibility

- Skip navigation link on every page (`<a href="#main-content" class="skip-link">`)
- All interactive elements are keyboard accessible
- `aria-label` and `aria-expanded` attributes on icon-only controls
- `role="presentation"` on decorative/duplicated images
- Animations respect `prefers-reduced-motion`
- Colour contrast meets WCAG AA requirements

---

## 🤝 Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md) for the full contribution guide.

**Quick summary:**

1. Fork the repository
2. Create a feature branch: `git checkout -b feature/my-change`
3. Follow the coding standards in [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md)
4. Run `npm run check` to lint and verify links before committing
5. Push and open a Pull Request against `main`

---

## 📄 License

This project is licensed under the **MIT License** — see the
[LICENSE](LICENSE) file for details.

---

## 👤 Author & Maintainer

**Zenith IT** — Website design and development  
**Jake Gibbons** — Project owner  
GitHub: [Jake-Gibbons](https://github.com/Jake-Gibbons)

---

*Last updated: April 2026*
